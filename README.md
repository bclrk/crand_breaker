# C's random number generator breaker

This python code will break GCC's random number generator. Given any sequence of 93+ numbers from the random number generator, this program predicts the next 93.
